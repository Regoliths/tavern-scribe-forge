apiVersion: v1
kind: Service
metadata:
  name: dndclient
  namespace: dungeonsanddragons
  labels:
    app: dnd
spec:
  selector:
    app: dndclient
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
  